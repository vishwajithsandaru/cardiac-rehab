services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=backenddb'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=test'
    ports:
      - '3306:3306'
